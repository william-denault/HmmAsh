% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spikedMattern.R
\name{fit_spike_matern_hmm}
\alias{fit_spike_matern_hmm}
\title{Fit a two-state Dirac--Matern Gaussian-process HMM}
\usage{
fit_spike_matern_hmm(
  y,
  se,
  position = seq_along(y),
  sequence_id = rep(1L, length(y)),
  nu = 1.5,
  nonnegative = FALSE,
  transition = NULL,
  stay_null = 0.995,
  stay_active = 0.98,
  init_prob = NULL,
  transition_prior = 1,
  init_prior = 1,
  estimate_transition = TRUE,
  estimate_init = FALSE,
  level = NULL,
  estimate_level = TRUE,
  gp_sd = NULL,
  range = NULL,
  estimate_gp_hyperparameters = TRUE,
  range_bounds = NULL,
  gp_sd_bounds = NULL,
  level_bounds = NULL,
  hyper_update_every = 5L,
  hyper_grid_size = 7L,
  minimum_active_weight = 1e-08,
  positive_inner_maxiter = 1L,
  positive_inner_tolerance = 1e-05,
  transition_floor = 1e-08,
  null_model_selection = c("bic", "none"),
  null_bic_margin = 0,
  maxiter = 100L,
  tolerance = 1e-05,
  verbose = FALSE
)
}
\arguments{
\item{y}{Numeric vector of noisy observations.}

\item{se}{Numeric vector of known positive observation standard errors.}

\item{position}{Increasing numeric coordinates. Defaults to equally spaced
positions `seq_along(y)`.}

\item{sequence_id}{Vector identifying independent contiguous sequences.}

\item{nu}{Matern smoothness. The exact state-space implementation supports
`0.5`, `1.5`, and `2.5`, with state dimensions one, two, and three.}

\item{nonnegative}{Logical; use the true nonnegative log-GP active effect
`exp(level + f_t)` instead of the signed identity link.}

\item{transition}{Optional initial 2 by 2 transition matrix. Rows correspond
to current states `(null, active)` and columns to next states.}

\item{stay_null, stay_active}{Initial self-transition probabilities used when
`transition` is `NULL`.}

\item{init_prob}{Initial probabilities for `(null, active)`. `NULL` uses the
stationary distribution of the initial transition matrix.}

\item{transition_prior}{Scalar, length-two vector, or 2 by 2 matrix of
Dirichlet parameters. Values must be at least one.}

\item{init_prior}{Scalar or length-two Dirichlet parameters used only when
`estimate_init = TRUE`.}

\item{estimate_transition}{Logical; learn transition probabilities.}

\item{estimate_init}{Logical; learn the initial state distribution.}

\item{level}{Initial GP level. In the nonnegative model this is on the log
scale. `NULL` uses a robust data-driven initialization.}

\item{estimate_level}{Logical; update the active GP level.}

\item{gp_sd}{Initial marginal GP standard deviation. In the nonnegative
model it is the standard deviation on the latent log scale.}

\item{range}{Initial Matern length scale in the units of `position`.}

\item{estimate_gp_hyperparameters}{Logical; learn GP variance and range.}

\item{range_bounds}{Optional positive lower and upper bounds for the Matern
range. `NULL` derives conservative bounds from the observed coordinates.}

\item{gp_sd_bounds}{Positive lower and upper bounds for `gp_sd`. `NULL`
chooses scale-aware bounds.}

\item{level_bounds}{Bounds for the level. The default is unbounded for the
signed model and `c(-20,20)` for the log-GP.}

\item{hyper_update_every}{Positive number of outer iterations between GP
hyperparameter updates.}

\item{hyper_grid_size}{Number of coarse log-range values used before bounded
one-dimensional refinement.}

\item{minimum_active_weight}{Active probabilities below this value are
omitted from the fractional Gaussian observation update.}

\item{positive_inner_maxiter}{Maximum Gauss--Newton smoothing steps per
outer iteration in the nonnegative model.}

\item{positive_inner_tolerance}{Relative convergence tolerance for those
inner steps.}

\item{transition_floor}{Small positive floor applied after transition
updates to prevent absorbing numerical states.}

\item{null_model_selection}{`"bic"` compares the variational full-model
score with the exact all-null likelihood; `"none"` always retains the
fitted switching model.}

\item{null_bic_margin}{Nonnegative extra margin required to retain the full
model over the exact null.}

\item{maxiter}{Maximum number of outer variational-EM iterations.}

\item{tolerance}{Relative convergence tolerance.}

\item{verbose}{Logical; print iteration diagnostics.}
}
\value{
An object of class `spike_matern_hmm_fit`. Important components are
  `posterior`, `state_probability`, `viterbi`, `transition`, `gp`,
  `history`, `elbo`, and `model_selection`.
}
\description{
Fits a sparse-and-smooth empirical-Bayes model in which state zero is the
exact point mass `beta_t = 0`, while state one exposes a latent Matern
Gaussian process. The Matern process is represented by an exact
low-dimensional state-space model, giving linear cost in the number of
observations for `nu` equal to `0.5`, `1.5`, or `2.5`.
}
\details{
The signed model is
\deqn{y_t\mid z_t,f_t \sim N(z_t(\mu+f_t),s_t^2),}
where `z_t` is a binary Markov chain and `f` is a zero-mean stationary
Matern GP. Thus `z_t=0` is exactly a Dirac effect at zero.

Exact marginalization of a GP inside a switching HMM produces an
exponentially growing Gaussian mixture. This function instead uses the
structured approximation
\deqn{q(z_{1:T},x_{1:T})=q(z_{1:T})q(x_{1:T}).}
Conditional on `q(z)`, `q(x)` is an exact Kalman/RTS posterior; conditional
on `q(x)`, `q(z)` is an exact two-state forward--backward posterior.
Transition probabilities, the GP level, variance, and Matern range are
updated by empirical Bayes. The signed updates are coordinate-ascent
variational EM.

With `nonnegative = TRUE`, the active effect is
\deqn{\beta_t=\exp(\mu+f_t)>0.}
Its HMM emission update uses exact log-normal moments under the Gaussian
approximation. The continuous update uses an iterated Gauss--Newton
Kalman smoother with an exact-objective line search. This is a stable
Laplace/IEKS approximation, not an exact variational coordinate update.

The latent GP evolves at every position, including while the Dirac state is
active. Correlation across a null stretch decays according to the Matern
range and the supplied position gaps. Independent sequences reset both the
GP and HMM recursions.
}
